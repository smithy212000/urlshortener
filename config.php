<?php
	$domain = "http://mydomain.com"; // Set this to the domain name of the server this script is running on. Must have http://
	$length = 5; // Length of the short URL sequence. Default: 5.
	$htmlRedirect = false; // Use HTML to redirect. Default: false - use php.
	$redirectLength = 0; // The redirect wait time for HTML redirects, doesn't work on all browsers. Default: 0.
?>
